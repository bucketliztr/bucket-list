  <div class="container">
    <div class="page-header" id="banner">
      <h1 class="amatic animated fadeIn">All Bucket Lists</h1>
    </div>

  <!-- Table -->
  <table class="table table-striped">
  <thead>
    <tr>
      <th><h4>Name</h4></th>
      <th><h4>Email</h4></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><strong><%= user.name %></strong></td>
        <td><strong><%= user.email %></strong></td>
        <td><%= link_to 'Show', user %>  |
        <%= link_to 'Edit', edit_user_path(user) %>  |
        <%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>


<br>
<%= if user.si
<h4><%= link_to 'Create Your Bucket List', new_user_path, class: "fittext pacifico" %></h4>

</div>
